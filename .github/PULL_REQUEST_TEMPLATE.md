## Summary

What does this change and why?

## Linked issue

Closes #

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes (new geometry/algorithms include unit tests)
- [ ] `npm run build` passes
- [ ] `npm run smoke` passes (required for canvas, rendering, or export changes)
- [ ] `CHANGELOG.md` updated under *Unreleased* (user-visible changes)

## Screenshots

Required for visual changes — before/after where applicable.

## Architecture notes

Call out anything touching the ground rules in CONTRIBUTING.md (document serializability, geometry purity, persistence seam, hit-testing conventions).
